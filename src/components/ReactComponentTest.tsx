import { css } from "../../styled-system/css";

export const ReactComponentTest = () => {
  return (
    <div
      className={css({
        background: "blue",
      })}
    >
      ReactComponentTest
    </div>
  );
};
